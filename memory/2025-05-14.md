
## 2025-05-14 명일방주: 엔드필드 PPT 제작 및 Strontium 업로드
- **작업 내용**: '명일방주: 엔드필드' 가이드를 담은 파워포인트(PPT) 파일을 Python `python-pptx` 라이브러리를 사용해 자동 생성함.
- **수정 사항**: 초기 버전(v1) 생성 후, 사용자 피드백을 반영하여 안젤리나(Angelina) 캐릭터에 대한 설명(오퍼레이터 안젤리나의 엔드필드 가드 협력 정보)을 추가하고 이미지를 포함한 v2를 제작함.
- **Strontium 연동**: 제작된 `Endfield_Summary_v2.pptx` 파일을 `strontium-webpage` 백엔드 업로드 디렉토리에 복사하고, SQLite DB(`strontium.db`)에 직접 쿼리를 날려 게시판 포스트로 등록함 (owner_id: 1).
- **기술적 특이사항**: 쉘 커맨드에서 따옴표 처리에 문제가 있어 별도의 Python 스크립트(`insert_message.py`)를 작성해 DB 인서트를 안전하게 수행함.
