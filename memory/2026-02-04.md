# 2026-02-04 Memory Log

## Summary of Session Activities
- **Backend Refactoring**: Successfully migrated the monolith backend to a **Hexagonal Architecture**. 
    - `core/`: Contains use cases (`AppService`, `KnowledgeService`, `UserService`, `ChatService`) and interfaces.
    - `adapters/`: Contains `SQLAlchemy` and `Auth` (JWT) implementations.
    - `main.py`: Acts as the FastAPI entry point.
- **RAG Implementation**: 
    - Integrated **Ollama** for embeddings (`nomic-embed-text`) and generation (`deepseek-r1:8b`).
    - Added vector search (cosine similarity via `numpy`) in the DB adapter.
    - Updated `documents` table schema with an `embedding` column (TEXT for JSON storage).
- **Environment & Bug Fixes**:
    - **Bcrypt Fix**: Resolved `AttributeError: module 'bcrypt' has no attribute '__about__'` by downgrading `bcrypt` to `4.0.1`. Fixed a `ValueError` regarding 72-byte password limits.
    - **Dependencies**: Manually installed `requests`, `numpy`, and `bcrypt==4.0.1`.
- **Infrastructure Migration**: 
    - Moved from Localtunnel to **Cloudflare Tunnel** to resolve persistent 503 errors and "511 Network Authentication Required" prompts.
    - Updated all frontend fetch calls to dynamic Cloudflare subdomains.

## Current System State
- **Frontend URL**: `https://architects-achieving-wales-accessed.trycloudflare.com`
- **Backend URL**: `https://panels-checks-stephen-watershed.trycloudflare.com`
- **Test Credentials**: `admin@admin.com` / `admin`
- **Active Models**: `nomic-embed-text` (Ollama), `deepseek-r1:8b` (Ollama).

## Future Tasks / Decisions
- Consider persistent Cloudflare tunnels (named tunnels) if quick tunnels continue to cycle.
- The database `agent_builder.db` was reset during schema changes; production data would require migration scripts.
