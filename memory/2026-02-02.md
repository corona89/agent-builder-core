# 2026-02-02 Memory Log

## Projects: Agent Builder
- **Core Goal**: Build an AI Agent construction platform (UI + Backend) for user `strontium00`.
- **Backend Architecture**: 
  - FastAPI + SQLAlchemy (SQLite).
  - Models: `User`, `App` (Agent config), `Knowledge` (Library), `Document` (RAG chunks with Parent-Child support).
  - Auth: JWT-based. Replaced `bcrypt` with `pbkdf2_sha256` due to environment compatibility issues with Python 3.11.
- **Frontend Architecture**:
  - Next.js 15 (App Router).
  - UI Library: Custom Shadcn/ui-inspired components using Tailwind CSS and Lucide-react.
  - Vibe: Dark mode, Professional Archive Core theme (#09090b background).
- **Environment & Deployment**:
  - Host IP: `211.208.216.146`.
  - External Access: Used `localtunnel` (https://cruel-foxes-slide.loca.lt) after `cloudflared` quick tunnels experienced repeated 500 errors.
  - Running Ports: Backend (8001), Frontend (3001).

## Credentials Created
- `admin@example.com` / `password123`
- `corona89@nate.com` / `New1234!`

## Status
- 1st phase of UI overhaul using Shadcn-style components completed.
- Verified UI visibility via internal browser screenshot.
- User pointed out that the previous UI was still lacking professional polish (perceived as broken/unstructured despite being "dark mode").
- Refined `globals.css` for Tailwind 4.0 compatibility and overhauled `page.tsx` with high-fidelity Zinc/Archive Core styling (deep blacks, refined borders, 30% opacity proprietary footers).
- Realized the "broken" feel was due to missing Shadcn configurations in the new Tailwind 4 structure.
