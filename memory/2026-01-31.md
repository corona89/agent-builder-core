# Durable Memory - 2026-01-31

## Strontium Project Fixes
- **Issue**: Strontium project's message board appeared empty after a Docker deployment.
- **Root Cause**: 
    - `strontium.db` was accidentally created as a directory on the host instead of a file.
    - Path mismatch in `database.py` vs `docker-compose.yml` environment variables.
- **Resolution**:
    - Renamed existing `sql_app.db` (containing previous markers) to `strontium.db`.
    - Updated `strontium-webpage/backend/database.py` to prioritize `DATABASE_URL` environment variable.
    - Re-synchronized Docker volumes and rebuilt the container.
- **Outcome**: Verified 6 posts successfully restored and accessible via the API.
- **Verification Details**: Since `sqlite3` was unavailable inside the container, used `docker exec strontium-webpage-strontium-1 python3 -c "import sqlite3; ..."` to confirm record count (6 messages).
- **Communication**: Reassured user that the 1.31 specific development logs (Kakao, MCP, PPT guide) are safe and persistence is hardened via proper volume mounting.
