# 2026-02-08 Session Memory - Lion Agent 🐌

## 🚀 Technical Achievements
- **Agent Builder Evolution**: 
    - Implemented a "Meeting Agent" type with built-in Web Speech API STT and conversation summarization.
    - Successfully refactored the Backend to a Hexagonal Architecture (core, adapters, interfaces). 
    - Fixed critical SQLAlchemy `OperationalError` and `ResponseValidationError` by syncing `models.py` and `schemas.py`.
    - Integrated local RAG via Ollama (`nomic-embed-text`) and DeepSeek-based generation.
- **Trello Monitor System**: 
    - Set up a robust polling script (`trello_monitor.py`) for the `AION-U` Trello board.
    - Configured a persistent cron job (`agentId: main`) using a Sub-Agent (`agentTurn`) approach for reliable Discord notifications.
- **Tunnel Management**:
    - Automated the injection of dynamic Cloudflare Tunnel URLs into the React frontend via `sed` scripts during deployment.

## 📋 User Context
- **User Preference**: Enjoys a playful, humorous "Secretary Lion" persona. Refers to me as a neural cluster/agent.
- **Active Projects**: AI Agent Builder (Self-hosted Agentic platform) and Trello-based workflow automation.

## 🔧 Infrastructure Note
- **Backend**: FastAPI running on port 8002.
- **Frontend**: Next.js running on port 3000.
- **Database**: SQLite (`agent_builder.db`). A manual migration script (`migrate_db.py`) was used to add `agent_type` column without full reset.

## 🧠 Lessons Learned
- When using Cloudflare quick tunnels, always automate the URL replacement in the frontend code; manual updates lead to high latency and connection friction.
- For cron-based notification tasks, spawning an isolated Sub-Agent is superior to `systemEvent` as it allows the agent to use proper tools (like `message`) for delivery.

## 🔄 Service Maintenance (2026-02-08 18:00)
- **Whisper STT**: CORS issue resolved by adding `CORSMiddleware` to allow all origins.
- **Action**: Rebuilt Docker image and restarted `whisper-stt` container.
